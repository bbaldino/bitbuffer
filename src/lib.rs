mod bit;
mod byte_buffer;
mod byte_buffer_slice;
mod error;
mod foo;
mod helpers;
mod parse_example;
mod readable_buf;
mod some_readable_buf;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
